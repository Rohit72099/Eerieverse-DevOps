define host{
    use                     linux-server
    host_name               eerieverse
    address                 <APP_PUBLIC_IP>
    check_interval          1
}
define service{
    use                     generic-service
    host_name               eerieverse
    service_description     HTTP
    check_command           check_http
}
